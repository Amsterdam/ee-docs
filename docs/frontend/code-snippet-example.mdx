import CodeBlock from '@theme/CodeBlock'
import TestComponent from '!!raw-loader!../../src/components/TestComponent/TestComponent';

# Code snippet examples

### Example of a React component snippet

<CodeBlock language="jsx">{TestComponent}</CodeBlock>

### An example of a CSS code snippet

```css
body {
  background-color: #f0f0f0;
  color: #333;
}

```

### An example of a JS code snippet

```js
const uri = 'https://mozilla.org/?x=шеллы';
const encoded = encodeURI(uri);
console.log(encoded);
// Expected output: "https://mozilla.org/?x=%D1%88%D0%B5%D0%BB%D0%BB%D1%8B"

try {
  console.log(decodeURI(encoded));
  // Expected output: "https://mozilla.org/?x=шеллы"
} catch (e) {
  // Catches a malformed URI
  console.error(e);
}
```

### An example of a Ruby code snippet

```ruby
require 'redcarpet'
markdown = Redcarpet.new("Hello World!")
puts markdown.to_html
```